/**
 * When all of this happened.
 *
 * Two timestamps in a file of their own, because half of what follows is dated from them:
 * the segments on the dashboard, the pictures taken during them and the log files a sweep
 * would carry off are all so many days either side of one evening, and they only line up if
 * they are counted from the same one.
 */

// Two evenings: the first is a keystone run followed two minutes later by an alt, which is
// the case that has to fold into one play session; the second is a day earlier and must not.
export const EVENING = 1785063600;
export const NIGHT_BEFORE = 1784977200;
