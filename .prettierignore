# TypeScript and TSX only — see the `format` scripts in package.json, which say so in their
# globs as well so that a bare `prettier .` cannot quietly widen the check.
#
# CSS and Markdown are deliberately left out rather than forgotten. The stylesheets are one
# small file per component and have no drift worth a tool; the Markdown is hand-wrapped prose
# whose line breaks are where somebody put them, and reflowing `CLAUDE.md` and `docs/` would be
# the high-volume churn that finds no defects.
*.css
*.md
*.json
*.html
*.yml
*.toml

# Generated by tauri-specta; `scripts/check-rust.sh` is what holds it to its source.
apps/desktop/src/bindings.ts

# Generated from `docs/game-tables.json` by `bun run tables:generate`;
# `scripts/game-tables.test.ts` is what holds it to its source.
scripts/tables.ts

# Not ours, or not text anybody edits.
**/node_modules/
**/dist/
**/target/
**/test-results/
**/playwright-report/
apps/desktop/fixtures/
bun.lock
